@{
    ViewData["Title"] = "Meddelanden";
    @model List<SOS100GroupProjectMVC.DTOs.Message>
}
<link rel="stylesheet" href="~/css/messages.css"/>
<div id="title-menu">
        <div>
            <h1>Inkorg</h1>
        </div>
        <div>
            <a class="btn btn-primary" asp-action="CreateMessage" asp-controller="Messages"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Skriv nytt meddelande</a>
        </div>
    </div> 
<div id="filter-menu">
    
</div> 
    <div id="messages-container">
        <div id="messages-scrollmenu">
            @{
                if (Model.Count > 0)
                {
                    foreach (var message in Model)
                    {
                        <div class="messages-preview"
                             id="@message.Id"
                             data-title="@message.Title"
                             data-content="@message.Content">
                        
                            <h3>
                                @message.timestamp.ToShortDateString()
                                @if (!message.IsRead)
                                {
                                    <i class="fa fa-exclamation-circle unread-icon" aria-hidden="true"></i>
                                }
                            </h3>
                            <h4>@message.Title</h4>
                            <p>@message.Content</p>
                        </div>
                    }
                }
                else
                {
                    <h2>Du har inga meddelanden att visa</h2>
                }
            }
        </div>
        <div id="selected-message">
            <h1 id="selected-message-title">Välj ett meddelande för att visa det här</h1>
            <div id="selected-message-body"></div><br/>
        </div>
    </div>
<script src="~/js/messages.js"></script>